;; UI config


;; Disable blinking cursor
(blink-cursor-mode -1)

;; Hide unused UI elements
(tool-bar-mode -1)
(menu-bar-mode -1)
(scroll-bar-mode -1)

;; Show line number
(global-linum-mode 1)

;; Highlight current line
(global-hl-line-mode 1)

(provide 'init-ui)
